// import { Session } from './Session';

describe('Session', () => {
  it('#updateFromPortal', () => {
    // const session = new Session('username', 'password', 'clientKey', 123);
    expect('teste').toBe('teste');
  });
});
